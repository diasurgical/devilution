language: cpp

notifications:
  email:
    on_failure: change # default: always

addons:
  apt:
    packages:
      #- gcc-mingw-w64
      - gcc-mingw-w64-tools
      - gcc-mingw-w64-i686

script:
  - make
