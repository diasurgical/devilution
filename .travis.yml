language: cpp

notifications:
  email:
    on_failure: change # default: always

addons:
  apt:
    packages:
      - gcc-mingw-w64
      - mingw-w64-x86_64
      - mingw-w64-x86_64-toolchain

script:
  - make
