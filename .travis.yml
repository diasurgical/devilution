language: cpp

notifications:
  email:
    on_failure: change # default: always

addons:
  apt:
    packages:
      - mingw-w64
      - mingw-w64-tools
      #- gcc-mingw-w64
      - gcc-mingw-w64-i686

before_script:
  - touch storm.dll

script:
  - make
